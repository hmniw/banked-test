{
  "name": "banked-test",
  "version": "1.0.0",
  "description": "A Cypress based test framework for testing the Open Weather Map website (https://openweathermap.org)",
  "scripts": {
    "cypress": "npx cypress run",
    "cypress:open": "npx cypress open",
    "cypress:headed": "npx cypress run --headed"
  },
  "author": "Will Herring",
  "devDependencies": {
    "cypress": "^9.0.0",
    "dotenv": "^10.0.0",
    "prettier": "2.4.1"
  }
}
